import { DateformatorPipe } from './dateformator.pipe';

describe('DateformatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateformatorPipe();
    expect(pipe).toBeTruthy();
  });
});
